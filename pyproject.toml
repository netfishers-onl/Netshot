[project]
name = "netshot-docs"
version = "0.1.0"
description = "Documentation toolchain for the Netshot docs site (Zensical + mike)"
requires-python = ">=3.11"
dependencies = [
    "mike",
    "zensical>=0.0.55",
]

[tool.uv]
package = false

[tool.uv.sources]
mike = { git = "https://github.com/squidfunk/mike.git" }
